import { ResponsiveContainer, LineChart, Line, CartesianGrid, XAxis, YAxis, Tooltip, Legend, BarChart, Bar } from 'recharts'
export function LinePlot({ data, xKey, lines }) { return <div className="h-64 w-full"><ResponsiveContainer><LineChart data={data} margin={{ top: 8, right: 12, left: -18 }}><CartesianGrid strokeDasharray="3 3" stroke="#e2e8f0"/><XAxis dataKey={xKey} tick={{ fontSize: 11 }}/><YAxis tick={{ fontSize: 11 }}/><Tooltip/><Legend/>{lines.map((line, i) => <Line key={line.key} type="monotone" dataKey={line.key} name={line.name} stroke={line.color || ['#0f766e','#f59e0b','#2563eb'][i]} dot={false} strokeWidth={2}/>)}</LineChart></ResponsiveContainer></div> }
export function BarPlot({ data, xKey, bars }) { return <div className="h-64 w-full"><ResponsiveContainer><BarChart data={data} margin={{ top: 8, right: 12, left: -18 }}><CartesianGrid strokeDasharray="3 3" stroke="#e2e8f0"/><XAxis dataKey={xKey} tick={{ fontSize: 11 }}/><YAxis tick={{ fontSize: 11 }}/><Tooltip/><Legend/>{bars.map((bar, i) => <Bar key={bar.key} dataKey={bar.key} name={bar.name} fill={bar.color || ['#0f766e','#f59e0b','#2563eb'][i]} radius={[4,4,0,0]}/>)}</BarChart></ResponsiveContainer></div> }
